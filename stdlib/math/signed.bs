#ifndef SIGNED_BS
#define SIGNED_BS

#define is_negative(n) \
    ((n) & 0x8000)

#define is_positive(n) \
    (!is_negative(n))

/*
#define gt(x, y) \
    (ge(x, y) && (x) != (y))

#define lt(x, y) \
    is_negative((x) - (y))
  
#define ge(x, y) \
    (!lt(x, y))

#define le(x, y) \
    (lt(x, y) || (x) == (y))
*/

#endif

