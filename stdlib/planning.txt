
/// Math

mul(x, y)
div(x, y)
mod(x, y)

// signed numbers

muln(x, y)
divn(x, y)
gen(x, y)
len(x, y)
gtn(x, y)
ltn(x, y)


/// Printing

// for: string, char, unsigned decimal, signed decimal, hexidecimal 16-bit, hexidecimal 8-bit

// print to lcd

put(str)
putc(char)
putd(integer)
putn(signed_integer)
putx(int)
putb(byte)

// print to string

sput(str_0, str_1)
sputc(str, char)
sputd
sputn
sputx
sputb

// cat to string

cat(str_0, str_1)
catc
catd
catn
catx
catb

